// determines if the levels are either all increasing or all decreasing.
const checkSortOrder = (arr) => {
 return true
}

// determines if any two adjacent levels differ by at least one and at most three.
const checkAdjacentDistances = (arr) => {
  return true
}

module.exports = { checkSortOrder, checkAdjacentDistances }